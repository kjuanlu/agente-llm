version: '3.8'

services:
  agente-llm:
    build: .
    container_name: agente-llm
    ports:
      - "8000:8000"
    environment:
      - OLLAMA_HOST=host.docker.internal:11434
    volumes:
      - .:/app
    restart: unless-stopped
